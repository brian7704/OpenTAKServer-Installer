# OpenTAKServer Installer Configuration
# Copy this file to .env and customize as needed

# GitHub Configuration
# Specify the GitHub user to download installer resources from
# This allows you to use a forked version of the installer
# Default: brian7704
OTS_GITHUB_USER=brian7704

# Development Mode
# Set to 1 to install from local source code instead of PyPI
# Useful for development and testing local changes
# Default: 0 (production mode)
OTS_DEV_MODE=0
OTS_DEV_PATH=$HOME/OpenTAKServer

# Installation paths
INSTALLER_DIR=/tmp/ots_installer
OTS_HOME=~/ots

# API Base Path
# Base path for API endpoints (e.g., 'opentakserver/' or '' for root)
# Note: Include trailing slash if not empty
# Default: '' (empty, API at root)
OTS_BASE=

# Optional features
# Set to 1 to skip interactive prompts and install automatically
INSTALL_ZEROTIER=0
ZT_NETWORK_ID=

INSTALL_MUMBLE=0

# PostgreSQL
# If set, will use this password instead of generating a random one
POSTGRESQL_PASSWORD=

# MediaMTX version
MEDIAMTX_VERSION=1.13.0
